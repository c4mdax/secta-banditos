---
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';

const values = [
  { title: "Comunidad", desc: "Apoyamos a todos los miembros, desde los veteranos de Vessel hasta los nuevos reclutas de Clancy." },
  { title: "Creatividad", desc: "Impulsamos el fan-art, la creación de merch propia y las dinámicas en los conciertos." },
  { title: "Respeto", desc: "Dema no tiene lugar aquí. Fomentamos un ambiente sano y libre de toxicidad." }
];
---

<Layout title="Sobre Nosotros - Secta Banditos">
  <Navbar />
  
  <main 
    class="relative bg-clancy-dark text-clancy-white min-h-screen py-24 px-6 overflow-hidden" 
    transition:name="main-content" 
    transition:animate="slide"
  >
    <div class="absolute top-0 right-0 w-1/3 h-full bg-gradient-to-l from-clancy-red/5 to-transparent pointer-events-none"></div>

    <div class="max-w-5xl mx-auto relative z-10">
      
      <header class="mb-20 reveal">
        <div class="flex items-center gap-4 mb-6">
          <div class="h-[1px] w-12 bg-clancy-red shadow-[0_0_8px_#ff0000]"></div>
          <span class="text-clancy-red font-black tracking-[0.5em] uppercase text-[10px] md:text-xs">

          </span>
        </div>
        <h1 class="text-7xl md:text-9xl font-heading leading-[0.8] uppercase tracking-tighter">
          Quiénes <br/>
          <span class="clancy-title-red">Somos</span>
        </h1>
      </header>

      <div class="grid md:grid-cols-2 gap-16 items-start mb-32">
        <div class="reveal-left space-y-8">
          <p class="text-2xl text-clancy-white font-bold leading-tight tracking-tight border-l-4 border-clancy-red pl-6 py-2">
            Secta Banditos no es solo un grupo de fans; es una comunidad unida por 
            la narrativa de Trench y el mensaje de Twenty One Pilots en México.
          </p>
          
          <div class="space-y-6 text-clancy-white/60 leading-relaxed font-light">
            <p>
              Nacimos en la clandestinidad de los grupos de comunicación, compartiendo teorías, 
              filtraciones y la emoción de cada era. Hoy, somos un colectivo que organiza 
              incursiones, intercambios de suministros y apoyo mutuo.
            </p>
            <p>
              Nuestra meta es que ningún Bandito se sienta solo en el campo de batalla. 
              Si llevas cinta amarilla o roja, <strong>eres uno de los nuestros.</strong>
            </p>
          </div>
        </div>

        <div class="mission-card group relative p-10 reveal-right bg-clancy-white/[0.03] border border-clancy-white/10 transition-all duration-500 hover:bg-clancy-red/5 hover:border-clancy-red/50">
          <div class="absolute -top-4 -left-4 w-12 h-12 border-t-2 border-l-2 border-clancy-red opacity-50"></div>
          
          <h3 class="text-3xl font-black mb-6 uppercase tracking-widest text-clancy-red">Nuestra Misión</h3>
          <p class="text-base leading-relaxed text-clancy-white/80 font-mono text-sm">
            Establecer un bastión seguro donde cada Bandito pueda expresarse y vivir la 
            experiencia de la banda más allá de los audífonos. En la Secta, el Clique es familia.
          </p>
          
          <div class="mt-10 flex gap-2">
             <div class="h-1 w-16 bg-clancy-red shadow-[0_0_10px_#ff0000]"></div>
             <div class="h-1 w-4 bg-clancy-white/20"></div>
             <div class="h-1 w-4 bg-clancy-white/20"></div>
          </div>
        </div>
      </div>

      <div class="grid sm:grid-cols-3 gap-8 mt-12 border-t border-clancy-white/10 pt-16 reveal delay-200">
        {values.map((val) => (
          <div class="value-item group relative border border-clancy-white/5 p-8 bg-gradient-to-b from-clancy-white/[0.02] to-transparent hover:border-clancy-red/30 transition-all duration-500 overflow-hidden">
            <div class="absolute bottom-0 right-0 text-clancy-red/5 font-black text-6xl translate-y-4 translate-x-4 group-hover:translate-y-0 group-hover:translate-x-0 transition-transform duration-700 pointer-events-none uppercase italic">
              {val.title.slice(0, 3)}
            </div>
            
            <h4 class="text-clancy-red font-black text-xl mb-4 uppercase tracking-[0.2em] group-hover:tracking-widest transition-all">
              {val.title}
            </h4>
            <p class="text-sm text-clancy-white/50 leading-relaxed italic relative z-10">
              "{val.desc}"
            </p>
          </div>
        ))}
      </div>

      <div class="absolute bottom-0 left-0 hidden lg:block border-l-2 border-b-2 border-clancy-red/20 w-32 h-32 pointer-events-none opacity-20"></div>

    </div>
  </main>

  <Footer />
</Layout>

<style>
  .clancy-title-red {
    color: #ff0000;
    transform: scaleY(1.15);
    display: inline-block;
    filter: drop-shadow(0 0 15px rgba(255, 0, 0, 0.5));
    letter-spacing: -0.05em;
    font-weight: 900;
  }

  .mission-card {
    clip-path: polygon(0 0, 100% 0, 100% 90%, 90% 100%, 0 100%);
  }

  .reveal {
    opacity: 0;
    transform: translateY(30px);
    animation: fadeInUp 0.8s forwards;
  }

  .reveal-left {
    opacity: 0;
    transform: translateX(-30px);
    animation: fadeInSide 1s forwards;
  }

  .reveal-right {
    opacity: 0;
    transform: translateX(30px);
    animation: fadeInSide 1s forwards;
  }

  @keyframes fadeInUp {
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeInSide {
    to { opacity: 1; transform: translateX(0); }
  }

  .delay-200 { animation-delay: 0.2s; }

  /* Efecto de ruido sutil en el fondo */
  main::before {
    content: "";
    position: absolute;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noiseFilter'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.65' numOctaves='3' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noiseFilter)'/%3E%3C/svg%3E");
    opacity: 0.02;
    pointer-events: none;
  }
</style>